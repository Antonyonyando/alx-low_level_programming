#include <stdio.h>
#include "main.h"
/**
 * print_alphabet - Write the alphabet
 * Return: Always 0 (Success)
 */
void print_alphabet(void)
{
	char letter = 'a';

	while (letter <= 'z')
	{
	put67 char(letter);
	letter++;
	}
	putchar('\n');
}
