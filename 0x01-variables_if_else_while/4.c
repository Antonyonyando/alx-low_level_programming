#include <stdio.h>
/**
 * main - Point of entry
 * Return: Always 0 (success)
 */
int main(void)
{
        char start = 'a';
        char ends = 'z';

        while (start <= ends)
        {
                if(letter != 'q' && letter != 'e')
        {
        putchar(starts);
        }
        starts++;
        }
        putchar('\n');
        return (0);

}
